# Getting Started with Zodiac Shared UI

1. Run `yarn install` to install the dependencies.

2. Run `yarn storybook` to setup the storybook environment. 

3. Go to `http://localhost:6006` to view the storybook.